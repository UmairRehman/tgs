import { api } from './api';

import * as users from './users';

import * as hr from './hr';

import { Storage } from './storage';

const services = {
    api,
    users,
    hr,
    Storage,
};

export default services;