import {
    makeStyles
} from "@material-ui/core";

const displayNoneStyles = makeStyles((theme) => ({
    DisplayNone: {
        display: 'none'
    }
}));

export const styles = {
    displayNoneStyles
};